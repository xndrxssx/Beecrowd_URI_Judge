#include <stdio.h>

int main (){
    int n, v[n], x, i, count;
    
    do{
        scanf("%d", &n);
    }while (!n);

    while (n)
    {
        do
        {
            scanf("%d", &x);
        } while (x<1 || x>2000);
        
        for (i = 0; i < count; i++)
        {
            /* code */
        }
        

    }
    
}