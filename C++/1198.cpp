#include <bits/stdc++.h>

using namespace std;

int main() {
    long long int a, b, r;
    while(cin>>a>>b){
        r=a-b;
            if (r<0)
                r*=-1;
        
        cout<<r<<endl;
    }
    return 0;
}
